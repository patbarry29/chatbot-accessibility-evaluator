declare const locale_en: {
  "act-rules": {
    [key: string]: {
      name: string;
      description: string;
      results: {
        [key: string]: string;
      };
    };
  };
  // Add other top-level properties here if they exist
};

export { locale_en };